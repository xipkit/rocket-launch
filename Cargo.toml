[package]
name = "kestrel"
version = "0.3.0"
edition = "2024"

[[bin]]
name = "sequencer"
path = "src/main.rs"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
